// Placeholder test suite. The default Flutter counter template test was removed
// when the package was renamed from flutter_app to go_wind_erp; real widget tests
// will be added alongside each feature module.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
